Task : OpenAI Codex 
      -Saying "Hello" (Python) 
      -Create random names (Python) 
      -Create a MySQL query (Python)

Ans : print("Hello")

import random first_names = ["John", "Alice", "Bob", "Eva"] last_names = ["Smith", "Johnson", "Miller", "Jones"] random_names = [random.choice(first_names) + " " + random.choice(last_names) for _ in range(10)] print(random_names)
